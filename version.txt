import os

# 1. Try to read the version file from the local directory
# (or download it from GitHub if running in Colab)
version_file = "version.txt"

if not os.path.exists(version_file):
    # If in Colab, grab the version.txt from your current branch
    # Note: You'll need to update this URL once to point to your repo
    !wget https://raw.githubusercontent.com/bboutelje/qox-python-samples/develop/version.txt

with open(version_file, 'r') as f:
    target = f.read().strip()

# 2. Execute the install based on the file content
if target == "develop":
    from google.colab import userdata
    token = userdata.get('GH_TOKEN')
    print("🚀 Dev Mode: Installing private library from develop branch...")
    !pip install git+https://{token}@github.com/bboutelje/qox.git@develop
else:
    print(f"✅ Stable Mode: Installing version {target} from PyPI...")
    !pip install qox
